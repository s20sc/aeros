name: dumpling.exec

inputs:
  task_graph: structured_plan

outputs:
  result: execution_report

preconditions:
  - task_graph.valid == true
  - arm.status == "ready"

postconditions:
  - dumpling.count >= 1

timeout_ms: 30000
retry: 2
