allowed_skills:
  - dumpling.plan
  - dumpling.exec

skill_permissions:
  dumpling.plan:
    actuators: []
    risk_level: low

  dumpling.exec:
    actuators:
      - arm
      - gripper
    risk_level: low
